import { CSSProperties } from "react";

export const logoDivStyle: CSSProperties = {
  display: "flex",
  alignItems: "center",
  flexDirection: "column",
  backgroundColor: "white",
};

export const menuStyle: CSSProperties = {
  height: "100vh",
};
